import { Button } from "@/components/ui/button";

const NavBtn = () => {
    return (
        <Button shimmer color="primary">
            Start A Free Trial
        </Button>
    );
};

export default NavBtn;